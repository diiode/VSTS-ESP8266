
credentials = require("credentials")
config = require("config")
build_results = require("build_results")
led_strip = require("led_strip")
app = require("app")
-- app_apa = require("app_apa")
setup = require("setup")

tmr.delay(10)
setup.start()
-- app_led_driver.start()
